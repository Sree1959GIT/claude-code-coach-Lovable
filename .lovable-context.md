# Lovable Handover Context
_Last updated: 2026-08-13 13:39 UTC · Session: Stage 6b — Admin & Quality_

## Project snapshot
Claude Code Architect Certification Tutor (CCAF): TanStack Start + Lovable Cloud
study app with an adaptive exam engine, RAG library and a multi-agent voice
mentor. Stages 1–6 are complete; we are mid **Stage 6b — Admin & Quality**
(sub-tasks 1–5 done of ~8).

## This session
- **Admin shell (6b·1)** — `/admin` route gated by an `admin` role read from `user_roles`.
- **Learners view (6b·2)** — admin-only aggregate of profiles, roles, attempts, accuracy, mastery cards, last activity.
- **Content view (6b·3)** — per-domain question audit with health flags (no single correct option, <2 options, missing explanations) plus attempt counts and accuracy.
- **Question authoring (6b·4)** — create / edit / delete questions and their options from the Content panel, via role-verified server functions using the service-role client.
- **Review queue schema (6b·5)** — `content_reviews` table with admin-only RLS and grants.

## Files changed
- `src/hooks/useIsAdmin.ts` — `useMyRoles` / `useIsAdmin` role hooks.
- `src/lib/admin.functions.ts` — `listLearners`, `listContent`, `saveQuestion`, `deleteQuestion`, `getQuestion`; all call `assertAdmin` (user-scoped `has_role`) before privileged reads/writes.
- `src/routes/_authenticated/admin.tsx` — admin console: Learners table, expandable Content panel, editor wiring.
- `src/components/admin/QuestionEditor.tsx` — question + options authoring form.
- `src/components/SiteHeader.tsx` — Admin nav link for admins.
- `.lovable/plan.md` — Stage 6b–9 roadmap.
- DB migration — `content_reviews` table, grants, admin-only policies.

## Env vars, secrets & schema
- Env/secrets: none added this session (existing `VITE_SUPABASE_*`, `LOVABLE_API_KEY` unchanged).
- Schema added: `public.content_reviews` (review queue: question ref, status, reviewer, notes, timestamps) with `GRANT`s to `authenticated`/`service_role` and admin-only RLS via `has_role(auth.uid(),'admin')`.
- Existing linter note about `has_role` in policies is intentional.

## Next step
Implement **Stage 6b sub-task 6** — the review queue UI in `/admin`: list pending
`content_reviews` rows with approve / reject actions backed by new admin-only
server functions (`listReviews`, `resolveReview`) in `src/lib/admin.functions.ts`.

## Open items
- Remaining Stage 6b sub-tasks: 6 (review queue UI), 7 (scheduled content health job), 8 (evaluation harness).
- Stage 7 (Exam Readiness) queued after 6b.
