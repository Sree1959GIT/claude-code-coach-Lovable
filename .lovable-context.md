# Lovable Handover Context
_Last updated: 2026-09-08 · Session: Phase E8 — advice-driven mentor depth_

## Project snapshot
Claude Code Architect Certification Tutor (CCAF Prep) — TanStack Start + Lovable Cloud
study app with Voice Mentor, RAG library, multi-agent authoring, readiness analytics,
and the Study Canvas code execution shell.

## This session (Phase E8)
- Added the missing `advice` jsonb column on `public.codebases` (default `{}`) in this
  remix's database; types regenerated.
- `src/lib/agents/advice-prompt.server.ts` (new) — `adviceDepth`, `adviceSystemMessage`,
  `adviceDepthDirective`. Depth = none | light | deep from advice richness, each with a
  pacing directive (3-4 / 3-5 / 5-7 sentences, misconception + follow-up usage).
- Explainer and Evaluator agents inject the advice system message after the question
  context; `QuestionContext.advice` added (server + `MentorCanvas`).
- `study.$slug.tsx` passes `codebaseQ.data?.advice` into `mentorContext`, so the mentor
  stream receives it via `/api/mentor-stream` unchanged.

## Next step
Phase E9 — tie canvas tabs to active question context profiles and FSRS metrics.

## Open items
- Only `agent_loop` was seeded with advice upstream; in this database no rows carry
  advice yet, so depth stays "none" until examples are regenerated through codegen.
