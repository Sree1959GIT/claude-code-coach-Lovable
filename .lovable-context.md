# Lovable Handover Context
_Last updated: 2026-09-09 · Session: Phase G2 — spider control desk_

## Project snapshot
Claude Code Architect Certification Tutor (CCAF Prep) — TanStack Start + Lovable Cloud
study app with Voice Mentor, RAG library, multi-agent authoring, readiness analytics,
and the Study Canvas code execution shell.

## This session (Phase G2)
- Migration: `public.crawl_targets` (url unique, optional `source_id`, label, tags,
  `enabled`, `crawl_interval_hours`, `last_crawled_at`, `last_ok`, `last_status`,
  `last_chars`, `last_chunks`) — admin-only read policy, service-role writes.
- Also applied the missing `import_runs` / `import_run_items` tables (Phase G1 schema
  existed upstream but not in this remix's database) — this cleared the type errors in
  `import.functions.ts` and `import-logs.functions.ts`.
- `src/lib/spider.functions.ts` (new) — list/add/update/delete crawl targets and
  `crawlTargets` (single or due batch, max 5 per run). Crawling reuses
  `source-fetch.server.ts` credentials and `ingest.server.ts` ingestion, then stamps
  the crawl timestamp/status/chunk counts.
- `src/components/admin/SpiderPanel.tsx` (new) + admin section 13 in
  `src/routes/_authenticated/admin.tsx`.

## Next step
Phase G4 — corpus defragmentation sweep.

## Open items
- Crawl runs are manual (Crawl_Due button); no cron schedule is wired yet.
