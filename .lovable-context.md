# Lovable Handover Context
_Last updated: 2026-09-10 · Session: Phase G5 — coverage parity report matrices_

## Project snapshot
Claude Code Architect Certification Tutor (CCAF Prep) — TanStack Start + Lovable Cloud
study app with Voice Mentor, RAG library, multi-agent authoring, readiness analytics,
and the Study Canvas code execution shell.

## This session (Phase G5)
- `src/lib/coverage.functions.ts` (new) — admin-gated `getCoverageParity`: per-domain
  blueprint share vs. bank share, signed delta, target count at a chosen bank size,
  gap, difficulty mix, citation share, and short/parity/over state.
- `src/components/admin/ParityPanel.tsx` (new) — target-bank-size slider, summary tiles,
  and a parity matrix with diverging bars.
- `src/routes/_authenticated/admin.tsx` — new section 15 · Coverage_Parity.
- No schema changes required; report is read-only.

## Next step
Phase F1 — model routing optimization (cache-first cheap models, tier-based escalation).

## Open items
- Parity targets are derived from `domains.weight`; keep those weights aligned with the
  official exam blueprint.
