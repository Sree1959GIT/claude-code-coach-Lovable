# Lovable Handover Context
_Last updated: 2026-09-07 · Session: Phase E7 — advice breakdown matrices_

## Project snapshot
Claude Code Architect Certification Tutor (CCAF Prep) — TanStack Start + Lovable Cloud
study app with Voice Mentor, RAG library, multi-agent authoring, readiness analytics,
and the Study Canvas code execution shell.

## This session (Phase E7)
- `advice` jsonb column on `public.codebases` (default `{}`), seeded for `agent_loop`.
- `src/lib/advice.ts` — advice types + `normalizeAdvice` / `hasAdvice`.
- Fifth codegen agent (`advice`) in `src/lib/codegen.server.ts`, persisted with the draft.
- `AdviceMatrix` panel + Advice/Code toggle and jump-to-line in `StudyCanvasTabs`.
- Admin tracker shows `05 · Advice`. Typecheck clean.

## Files changed
- `src/lib/advice.ts` (new), `src/components/AdviceMatrix.tsx` (new)
- `src/lib/codegen.server.ts`, `src/lib/codebases.ts`,
  `src/components/StudyCanvasTabs.tsx`, `src/routes/_authenticated/study.$slug.tsx`,
  `src/components/admin/CodeGenPanel.tsx`, `AGENTS.md`

## Next step
Phase E8 — feed advice matrices into Mentor agent prompts for adaptive depth.

## Open items
- Only `agent_loop` has seeded advice; other cached examples show no Advice toggle
  until regenerated through the codegen loop.
