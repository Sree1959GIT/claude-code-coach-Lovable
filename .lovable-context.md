# Lovable Handover Context
_Last updated: 2026-09-14 · Session: Phase F4 — encrypted BYOK key vault_

## Project snapshot
Claude Code Architect Certification Tutor (CCAF Prep) — TanStack Start + Lovable Cloud
study app with Voice Mentor, RAG library, multi-agent authoring, readiness analytics,
the Study Canvas code execution shell, and cost/usage observability.

## This session (Phase F4)
- New table `user_provider_keys` (service-role only) holding AES-256-GCM ciphertext of
  each user's Anthropic / Google key, plus last-4, label, active flag, verify status.
- New secret `BYOK_ENCRYPTION_KEY` (auto-generated) is the vault key source.
- `src/lib/byok.server.ts` (new) — crypto helpers, shape validation, provider
  verification, CRUD, and `getActiveKey()` for server-side consumption.
- `src/lib/byok.functions.ts` (new) — auth-scoped server functions; raw keys never
  leave the server.
- `src/components/admin/ByokPanel.tsx` (new) + admin section 17 · BYOK_Key_Vault.

## Next step
Phase F5 — validation gates: use a stored active key in the worker engines, with
fallback to the Lovable AI Gateway.

## Open items
- Stored keys are not yet used by any completion path; that is F5.
